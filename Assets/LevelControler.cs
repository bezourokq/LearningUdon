using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class LevelControler : MonoBehaviour
{
    
    void Start()
    {
    }

    public void StartGame()
    {
        
    }

    private void setPlayerH(GameObject temp,float gameF)
    {
        try
        {
            //temp.transform.position = new Vector3(temp.transform.position.x, gameF + playerH, temp.transform.position.z);
        }
        catch
        {

        }
       
    } 


    void Update()
    {
    
    }

}
